FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY train_lstm.py .

RUN mkdir -p /checkpoints /runs

CMD ["python", "-u", "train_lstm.py"]
